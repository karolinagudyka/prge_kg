name: build and push and deploy
on:
  push:
    branches:
      - env-test

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    name: Build & push
    runs-on:
      - self-hosted
    steps:
      - uses: actions/checkout@v3

      - name: Print
        run: echo 'witaj'